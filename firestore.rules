rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // 🔒 SECURITY: Allow only authenticated users to read/write
      allow read, write: if request.auth != null;
    }
  }
}
